
  # Scroll-driven camera animation

  This is a code bundle for Scroll-driven camera animation. The original project is available at https://www.figma.com/design/XgfOJqCtZz5TOIH68jdFLc/Scroll-driven-camera-animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  